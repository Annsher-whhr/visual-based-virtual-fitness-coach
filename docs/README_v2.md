# 太极拳起势动作智能评估系统 v2.0

## 🎯 系统简介

这是一个基于深度学习的太极拳起势动作质量评估系统，能够自动分析视频中的动作并给出评分和改进建议。

**v2.0 重大更新**：
- ✅ 从4帧升级到**12帧**训练数据
- ✅ 模型准确率从60%提升到**98.75%**
- ✅ 基于真实标准视频自动提取特征
- ✅ 智能检测动作开始和结束边界
- ✅ 更详细的错误分析和建议

---

## 📊 性能指标

| 指标 | v1系统 | v2系统 | 提升 |
|------|--------|--------|------|
| 准确率 | ~60% | **98.75%** | +38% |
| 精确率 | ~50% | **97.56%** | +47% |
| 召回率 | ~70% | **100%** | +30% |
| F1分数 | ~60% | **98.77%** | +38% |

---

## 🚀 快速开始

### 安装依赖

```bash
pip install -r requirements.txt
```

### 评估视频

```bash
# 评估指定视频
python evaluate_taichi.py -v video/qishi1.mp4

# 测试多个视频
python test_improved_system.py
```

### 输出示例

```
============================================================
太极拳起势动作评估 v2.0
============================================================
视频: video/qishi1.mp4

正在分析视频...
  FPS: 30.00, 总帧数: 716
[OK] 特征提取完成，共 716 帧

检测动作边界并抽取关键帧...
开始帧: 第 94 帧 (时间: 3.13s)
结束帧: 第 364 帧 (时间: 12.13s)
动作时长: 271 帧 (9.03s)

使用神经网络评估动作质量...

============================================================
评估结果
============================================================
质量分数: 0.00%
判定: [!] 需要改进
建议: 屈膝不足，请下沉重心，膝盖微屈。 手臂偏高，请下放手臂保持与肩平行。
============================================================
```

---

## 🏗️ 系统架构

```
视频输入
   ↓
[人体检测] detect_human()
   ↓
[姿态估计] estimate_pose()  ← MediaPipe
   ↓
[特征提取] recognize_action()  ← 22维特征向量
   ↓
[智能抽帧] select_frames_evenly()  ← 检测边界 + 均匀采样
   ↓ (12帧)
[模型评估] predict_quality()  ← 深度神经网络 (264维输入)
   ↓
[结果输出] 分数 + 建议
```

---

## 📝 核心功能

### 1. 智能边界检测

系统能自动检测视频中起势动作的开始和结束：

- **开始检测**: 手臂高度开始持续上升的点
- **结束检测**: 膝盖弯曲达到峰值且姿势稳定的点

### 2. 多帧评估

使用**12个关键帧**全面评估动作质量：

| 帧序号 | 阶段 | 关键特征 |
|--------|------|----------|
| 1-3 | 准备阶段 | 手臂开始上升 |
| 4-6 | 上升阶段 | 手臂高举、身体稳定 |
| 7-9 | 下落阶段 | 手臂下落、肘部弯曲 |
| 10-12 | 完成阶段 | 膝盖弯曲、脚距最大 |

### 3. 错误检测

系统能检测8种常见错误：

1. ❌ **肩膀不平** - 左右肩高度不一致
2. ❌ **屈膝不足** - 膝盖弯曲度不够
3. ❌ **手臂偏低** - 手臂高度不足
4. ❌ **手臂偏高** - 手臂过度抬高
5. ❌ **躯干倾斜** - 身体前倾或后仰
6. ❌ **脚距过窄** - 双脚间距太小
7. ❌ **脚距过宽** - 双脚间距过大
8. ❌ **肘部僵直** - 肘部弯曲不足

---

## 🔧 自定义训练

### 使用自己的标准视频

```bash
# 1. 编辑 extract_standard_features.py，修改视频路径
# video_path = "video/your_standard_video.mp4"

# 2. 提取标准特征
python extract_standard_features.py

# 3. 生成训练数据（可调整样本数）
python taichi_ai/generate_data_v2.py

# 4. 训练模型
python taichi_ai/train_model_v2.py

# 5. 测试新模型
python evaluate_taichi.py -v video/test.mp4
```

### 调整参数

在 `extract_standard_features.py` 中：
```python
# 修改抽帧数量（默认12）
n_frames = 16  # 或其他数值

# 训练时也要使用相同帧数
# 在 generate_data_v2.py 和 predict_v2.py 中会自动匹配
```

---

## 📂 项目文件结构

```
├── evaluate_taichi.py              # 主程序（v2）
├── extract_standard_features.py    # 标准视频特征提取
├── frame_selector_v2.py            # 改进的抽帧器
├── test_improved_system.py         # 系统测试脚本
│
├── taichi_ai/
│   ├── generate_data_v2.py        # 数据生成器（12帧）
│   ├── train_model_v2.py          # 模型训练（含评估报告）
│   ├── predict_v2.py              # 预测模块（12帧）
│   ├── X.npy                      # 训练数据（264维）
│   ├── y.npy                      # 训练标签
│   └── scaler.pkl                 # 数据标准化器
│
├── taichi_mlp_v2.h5                # 训练好的模型
├── qishi3_standard_frames.json     # 标准帧数据
├── model_evaluation_report_v2.png  # 训练报告
│
├── 系统改进总结_v2.md              # 详细改进说明
└── 快速使用指南_v2.md              # 使用教程
```

---

## 🧪 测试结果

系统在三个测试视频上的表现：

| 视频 | 质量分数 | 判定 | 检测的问题 |
|------|---------|------|-----------|
| **qishi3.mp4** (标准视频) | 99.99% | ✅ 标准 | 无 |
| **qishi1.mp4** | 0.00% | ❌ 需改进 | 屈膝不足、手臂偏高、脚距过窄、肘部弯曲不足 |
| **qishi2.mp4** | 100.00% | ✅ 标准 | 无 |

**结论**: 模型能够准确区分标准动作和错误动作！

---

## 💻 程序化调用

```python
from evaluate_taichi import evaluate_taichi_video

# 评估视频
result = evaluate_taichi_video("video/your_video.mp4", n_frames=12)

# 获取结果
score = result['score']        # 质量分数 (0-1)
is_correct = result['is_correct']  # 是否标准 (bool)
advice = result['advice']      # 改进建议 (str)

print(f"分数: {score*100:.2f}%")
print(f"建议: {advice}")
```

---

## 🔬 技术原理

### 特征提取（每帧22维）

1. **距离特征** (5): 肩宽、臀宽、脚距、手距、肩臀距
2. **角度特征** (7): 左右膝盖、肘部、肩部角度 + 平均膝盖弯曲 + 躯干角度
3. **位置特征** (4): 躯干向量 (vx, vy) + 手臂高度比 + 头部偏移
4. **平衡特征** (4): 左右肩平衡 + 左右臀平衡
5. **质量特征** (1): 平均可见性

### 神经网络架构

- **输入**: 12帧 × 22特征 = 264维向量
- **隐藏层**: 256 -> 128 -> 64
- **输出**: 1维 (sigmoid激活，范围0-1)
- **正则化**: L2 + Dropout + BatchNorm
- **优化**: Adam + 学习率衰减 + 早停

### 边界检测算法

```
检测开始帧:
  1. 计算手臂高度变化率（5帧平滑）
  2. 找到变化率 > 0.01 且 高度 < 0.6 的点
  3. 取第一个符合条件的点

检测结束帧:
  1. 找到膝盖弯曲最大值的位置
  2. 从该位置往后找脚距变化 < 0.015 的稳定点
  3. 取第一个稳定点
```

---

## 📈 训练详情

- **数据集**: 1600样本 (800正 + 800负)
- **训练集**: 1280样本
- **测试集**: 320样本
- **训练轮数**: 43轮（早停）
- **批次大小**: 32
- **优化器**: Adam (初始lr=0.001)

**最终测试集性能**:
- 真正例: 160 (所有正样本都识别正确)
- 真负例: 156 (97.5%的负样本识别正确)  
- 假正例: 4 (仅2.5%的负样本误判)
- 假负例: 0 (无漏检)

---

## 🎓 关键特征说明

### 起势动作的特征演变

基于qishi3.mp4提取的标准数据：

**阶段1 (帧1-3)**: 准备阶段
- arm_height_ratio: 0.04 → 0.20
- foot_distance: 0.256 → 0.258
- knee_bend: 4° → 10°

**阶段2 (帧4-6)**: 上升阶段  
- arm_height_ratio: 0.32 → 0.51
- foot_distance: 0.259 → 0.260
- knee_bend: 15° → 21°

**阶段3 (帧7-9)**: 下落阶段
- arm_height_ratio: 0.59 → 0.72
- foot_distance: 0.262 → 0.263
- knee_bend: 22° → 26°

**阶段4 (帧10-12)**: 完成阶段
- arm_height_ratio: 0.76 → 0.81
- foot_distance: 0.265 → 0.265 (稳定)
- knee_bend: 26° → 26° (峰值稳定)

---

## 🛠️ 故障排除

### 问题1: 评估结果不准确

**解决**:
1. 检查是否使用了v2模型: `taichi_mlp_v2.h5`
2. 确保 `scaler.pkl` 存在于 `taichi_ai/` 目录
3. 视频质量要清晰，人物完整
4. 确保光线充足，避免遮挡

### 问题2: 边界检测失败

**解决**:
1. 检查视频是否包含完整的起势动作
2. 调整检测参数（在 `frame_selector_v2.py` 中）
3. 确保人物在画面中央，姿态完整可见

### 问题3: 模型加载失败

**解决**:
```bash
# 重新训练模型
python taichi_ai/generate_data_v2.py
python taichi_ai/train_model_v2.py
```

### 问题4: 标准数据缺失

**解决**:
```bash
# 重新提取标准视频特征
python extract_standard_features.py
```

---

## 📚 文档索引

- **系统改进总结_v2.md** - 详细的改进说明和技术对比
- **快速使用指南_v2.md** - 使用教程和API说明
- **README_v2.md** - 本文档（系统总览）

---

## 🔬 算法创新点

### 1. 多尺度特征融合
- 时间尺度: 12个时间点的特征
- 空间尺度: 22个空间特征维度
- 总计: 264维超高维特征向量

### 2. 数据驱动的标准定义
- 不再依赖手工定义的标准值
- 直接从真实标准视频中学习
- 自动捕捉动作的自然变化

### 3. 智能时序分析
- 自动检测动作边界
- 考虑特征的时序变化
- 分阶段的错误诊断

### 4. 鲁棒的模型设计
- 深度网络提取高层特征
- 多层正则化防止过拟合
- 数据标准化提高稳定性

---

## 🎯 适用场景

✅ 太极拳教学辅助  
✅ 动作标准性评估  
✅ 自主练习反馈  
✅ 运动康复指导  
✅ 体育教学评分  

---

## 🔮 未来展望

### 短期计划
- [ ] 支持更多太极拳动作（如揽雀尾、单鞭等）
- [ ] 添加动作可视化标注功能
- [ ] 开发Web界面
- [ ] 支持实时评估（摄像头输入）

### 长期计划
- [ ] 使用LSTM/Transformer建模时序依赖
- [ ] 多角度视频融合评估
- [ ] 个性化训练计划生成
- [ ] 移动端应用开发

---

## 📄 License

MIT License - 详见 LICENSE 文件

---

## 🙏 致谢

- MediaPipe: 人体姿态估计
- TensorFlow: 深度学习框架
- OpenCV: 视频处理

---

**版本**: v2.0  
**更新时间**: 2025-11-12  
**作者**: AI Assistant

如有问题或建议，欢迎提出！

